// Re-export slug matcher for semantic clarity in directory routes
// The route structure [directory=dir] vs [...slug=slug] handles single vs multi-segment matching
export { match } from "./slug.js";
